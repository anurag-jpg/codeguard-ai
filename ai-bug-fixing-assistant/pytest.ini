[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --tb=short
    -v
    --cov=backend
    --cov=ai_engine
    --cov-report=term-missing
    --cov-report=html:coverage_html
    --cov-fail-under=60
filterwarnings =
    ignore::DeprecationWarning
